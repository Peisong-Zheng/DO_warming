**完善版总结**

1. 本研究的核心目标是检验 Rousseau et al. 2023 提取的 weak/strong monsoon start timing 是否偏离随机发生，并进一步判断这种 timing 是否与轨道强迫，尤其是岁差相位，以及冰量/温室气体背景状态有关。更准确地说，本研究不是直接分析 Greenland DO 事件本身，而是分析中国石笋记录中的 monsoon transition timing，这些 transition 可被解释为 DO-like 气候转换在亚洲季风系统中的对应表现。

2. 数据使用 Rousseau et al. 2023 给出的 strong monsoon starts 和 weak monsoon starts，原始基础来自 Cheng et al. 2016 中国石笋 δ18O 合成记录。样本量为 strong monsoon starts `N=96`，weak monsoon starts `N=94`，分析区间为 `0-640 ka BP`。其中 weak monsoon starts 可理解为向弱季风/Greenland stadial-like 状态转换，strong monsoon starts 可理解为向强季风/Greenland interstadial-like 状态转换。

3. [analyze_rousseau2023_monsoon_randomness_lohmann.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_randomness_lohmann.py) 复现 Lohmann-style moving-window event-count 检验，用固定速率 Poisson 过程和 fixed-N 随机过程作为 null model，检验事件 timing 是否可由平稳随机过程解释。结果显示 weak monsoon starts 明显偏离随机发生：Poisson-rate null `p=0.013`，fixed-N null `p=0.0044`。strong monsoon starts 对 fixed-N null 显著：`p=0.045`，但对 Poisson-rate null 只是边缘不显著：`p=0.083`。因此更严谨的表述是：weak 事件序列稳定拒绝平稳随机发生假说；strong 事件序列在固定事件数条件下也显示非随机聚集/间歇性，但对允许事件总数波动的 Poisson null 证据较弱。

4. [analyze_rousseau2023_monsoon_orbital_phase_rayleigh.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_orbital_phase_rayleigh.py) 检验事件 timing 是否集中在特定轨道相位。这里检验的是 **precession index** 和 **obliquity** 相位，不是 eccentricity 相位。相位约定为 precession index minimum = `0°`，maximum = `180°`。Rayleigh test 显示 strong 和 weak monsoon starts 都显著集中在岁差相位上：strong `p=0.0019`，平均相位约 `21.9°`；weak `p=0.0054`，平均相位约 `226.7°`。相比之下，obliquity 相位不显著：strong `p=0.682`，weak `p=0.632`。这说明事件 timing 与岁差相位有关，而没有证据表明其直接依赖 obliquity 相位。

5. [analyze_rousseau2023_monsoon_bin_hazard_phase_poisson.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_bin_hazard_phase_poisson.py) 使用 `0.2 kyr` bin 的 Poisson hazard GLM 建模事件率：

```text
Y_i ~ Poisson(lambda_i * dt)
log(lambda_i) = beta0 + beta1 LR04_i + beta2 CO2_i
                + beta3 sin(pre_phase_i) + beta4 cos(pre_phase_i)
```

结果显示，`LR04 + CO2 + precession phase` 是 strong 和 weak 两组事件的 AICc 最优核心模型。加入 precession phase 后，相比只含 LR04+CO2 的模型显著改进：strong `p=5.25e-4`，ΔAICc = `-11.09`；weak `p=0.00485`，ΔAICc = `-6.65`。GLM 估计的岁差偏好相位与 Rayleigh test 互相印证：strong 的 preferred phase 约 `10.8°`，weak 约 `237.5°`。对应的最大/最小事件率比约为 strong `3.18`，weak `2.63`。

6. [analyze_rousseau2023_monsoon_bin_hazard_phase_poisson_sensitivity.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_bin_hazard_phase_poisson_sensitivity.py) 在基准模型 `LR04 + CO2 + precession phase` 之外加入其他潜在驱动，包括 Antarctic temperature、obliquity、eccentricity 和 65°N summer-solstice insolation。结果显示这些额外变量没有提供稳健的独立贡献。联合加入所有额外变量后，strong `p=0.682`，weak `p=0.733`，且 AICc 变差。单独加入 AT、obliquity、eccentricity 或 65N insolation 的 p 值也均不显著。这说明在当前模型框架下，主要可识别的轨道信号来自岁差相位，而不是 eccentricity 或 obliquity 的独立线性贡献。

7. [analyze_rousseau2023_monsoon_bin_hazard_phase_poisson_binwidth_sensitivity.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_bin_hazard_phase_poisson_binwidth_sensitivity.py) 检验 bin width 选择是否影响结论。扫描 `0.2-1.0 kyr` 后，核心结论完全稳定：所有 bin width 下，`LR04 + CO2 + precession phase` 都是核心模型中的 AICc 最优模型；precession phase after LR04+CO2 均显著；preferred phase 相对 `0.2 kyr` 结果的偏移小于 `1°`。因此 `0.2 kyr` bin width 是合理且偏保守的选择：虽然空 bin 比例高约 `97%`，但几乎没有 multi-event bin，更符合 bin 内事件率近似恒定的假设。

8. [analyze_rousseau2023_monsoon_lagged_te_predictive_info.py](/Users/pz/VScode/DO_warming/analyze_rousseau2023_monsoon_lagged_te_predictive_info.py) 使用 model-based TE-like predictive information，而不是传统非参数 TE。其核心指标是嵌套 Poisson hazard 模型的 log-likelihood gain，并转换为 bits/event。该脚本检验 LR04、CO2 和 precession phase 在不同 lag 下对事件发生是否提供额外预测信息。结果显示 strong monsoon starts 中，LR04 和 CO2 的最佳单变量 lag 均约为 `4 kyr`，而 precession phase 的最佳 lag 为 `0 kyr`；weak monsoon starts 中，CO2 最佳 lag 约 `0.6 kyr`，LR04 和 precession phase 约 `0 kyr`。在控制 LR04 和 CO2 后，precession phase 仍在近零 lag 附近提供显著额外预测信息。这个结果说明岁差信号更像是相位门控或同步条件，而 LR04/CO2 代表的背景气候状态可能体现较慢的调制时间尺度。

**一句话总结：**

> 本研究表明，Rousseau et al. 2023 的 weak/strong monsoon transition timing 不能充分由平稳随机过程解释，并且在多个独立统计框架下均显示出对岁差相位的稳健依赖；这种岁差信号在控制 LR04 和 CO2 所代表的冰量/温室气体背景状态后仍然显著，而 obliquity、eccentricity、Antarctic temperature 和 65°N 夏至日照未显示出额外的独立解释力。